# dev
mysql -u root -p < schema_dev.sql
mysql -u root -p chat_dev < schema.sql

# homol
mysql -u root -p < schema_homol.sql
mysql -u root -p chat_homol < schema.sql

# prod
mysql -u root -p < schema_prod.sql
mysql -u root -p chat_prod < schema.sql